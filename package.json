{
  "name": "hls-extractor",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^23.4.1"   // ✅ use full puppeteer, not puppeteer-core
  }
}
